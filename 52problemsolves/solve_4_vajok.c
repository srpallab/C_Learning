#include <stdio.h>
#include <math.h>

int main()
{
  int i, N, m;
  scanf("%d", &N);
  int A[N];
  for (i = 0; i < N; ++i) {
    A[i] = scanf("%d", &m);
  }
  for (i = 0; i < sqrt(A[i]); ++i) {
    if (A[i] % i == 0) {
      if (i) {
	
      }
    }
  }


  return 0;
}

